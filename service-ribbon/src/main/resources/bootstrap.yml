spring:
  application:
    name: service-ribbon
  zipkin:
    base-url: http://localhost:9411

server:
  port: 1201

management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/
